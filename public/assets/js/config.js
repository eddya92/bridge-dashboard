var primary = localStorage.getItem("primary") || '#666666';
var secondary = localStorage.getItem("secondary") || '#b09750';

window.CubaAdminConfig = {
	// Theme Primary Color
	primary: primary,
	// theme secondary color
	secondary: secondary,
};
