var primary = localStorage.getItem("primary") || '#0099cc';
var secondary = localStorage.getItem("secondary") || '#45b089';

window.CubaAdminConfig = {
	// Theme Primary Color
	primary: primary,
	// theme secondary color
	secondary: secondary,
};
